
<%= form_for :student_period do |f|%>

  <% f.label :period_id %>
  <% f.text_field :period_id, required:true, value: @period_id %>

    <%= f.label :student_id, "Select Students" %>
    <%= f.select :student_id, options_for_select(@student)%>
  
  <%= f.submit "Submit"%>

  <%end%>