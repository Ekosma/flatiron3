
<%= form_for :student_period do |f|%>

  <%= f.label :period_id%>
  <%= f.text_field :period_id, required:true%>

    <%= f.label :student_id%>
  <%= f.text_field :student_id, required:true%>

  <%= f.submit "Submit"%>

  <%end%>